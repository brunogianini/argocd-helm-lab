apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meuapp.fullname" . }}-test-connection"
  labels:
    {{- include "meuapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meuapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
