apiVersion: v1
kind: Service
metadata:
  name: {{ include "meuapp.fullname" . }}
  labels:
    {{- include "meuapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meuapp.selectorLabels" . | nindent 4 }}
